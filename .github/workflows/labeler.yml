name: "Pull Request Labeler"

on:
  pull_request:
    types:
      - opened
      - review_requested  
  pull_request_review:
    types:
      - submitted

permissions:
  contents: read
  pull-requests: write

jobs:
  # dump_contexts_to_log:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/github-script@v6
  #       with:
  #         script: |
  #           const labels = await github.rest.issues.listLabelsOnIssue({
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             issue_number: context.issue.number,
  #           })
  #           console.log(labels.data)

  label-changes-requested:

    runs-on: ubuntu-latest

    steps:
      - if: github.event.review.state == 'changes_requested'
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['changes-requested']
            });

      - if: github.event.action == 'review_requested'
        uses: actions/github-script@v6
        with:
          script: |
            const { data: labels } = await github.rest.issues.listLabelsOnIssue({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number
            });

            const labelToRemove = 'changes-requested';

            if (labels.some(label => label.name === labelToRemove)) {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                name: labelToRemove
              });
            }

  label-invalid-branch-name:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            const regex = /^(releases\/sprint-[0-9]+|feature\/s[0-9]+\/[a-z0-9-]+|develop|qa|staging)$/;
            const branchName = context.payload.pull_request.head.ref;

            if (!regex.test(branchName)) {
              await github.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                labels: ['branch-name-fix-needed']
              });
            }
              